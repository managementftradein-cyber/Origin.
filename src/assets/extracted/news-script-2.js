(async()=>{let x=document.getElementById('news');try{let r=await fetch('/api/news'),j=await r.json();let d=j.items||[];x.innerHTML=d.length?d.map(n=>`<article><div class="tcc-meta">${n.created_at?new Date(n.created_at).toLocaleDateString(undefined,{year:'numeric',month:'short',day:'numeric'}):''}</div><h2>${n.title||''}</h2><p>${n.excerpt||n.content||''}</p></article>`).join(''):'<div class="tcc-empty">No news has been published yet.</div>'}catch(_){x.innerHTML='<div class="tcc-empty">News will appear here when the backend is connected.</div>'}})()
